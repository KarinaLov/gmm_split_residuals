# Python Requirements for GMM Functions
# Python version: 3.13

numpy>=2.0.0
pandas>=2.0.0
polars>=1.0.0
matplotlib>=3.8.0
scipy>=1.13.0
geopandas>=1.0.0
shapely>=2.0.0
pymer4>=0.9.0

# Note: pymer4 fits the lme4/lmerTest models via rpy2 + R under the hood
# (it brings its own conda-installed R + R packages), but this code no
# longer talks to rpy2 directly - only via pymer4.models.lmer.
# R version used: 4.5.3
# Required R packages are listed in R_requirements.txt
